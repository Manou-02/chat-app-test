
export type PaginationType = {
      current: number;
      currentPerPage: number;
      first: number;
      itemsPerPage: number;
      last: number;
      links: Array<string>;
      next: number;
      previous: number;
      totalItems: number;

}